# Complete Kanato Website Build

This is a code bundle for the Complete Kanato Website.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
